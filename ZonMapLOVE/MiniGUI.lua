




-- Return a GUILayer
function NewGUILayer(x, y, Scale)
    local A = {
        Placment = {["x"] = x, ["y"] = y, ["s"] = Scale},
        Theame   = {},
        Elements = {}
    }

    

end



function CreateButton(Lable, x, y, Hight, Width OnClick)




end
